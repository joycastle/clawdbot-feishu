# 2026-02-24

## 凭证恢复行动

### 起因
上下文压缩失败导致 session 截断，发现 Jenkins 脚本丢失。

### 恢复过程

1. **Jenkins 脚本** - 从 `~/.clawdbot-260216/agents/main/sessions/*.jsonl` 挖出完整脚本和凭证
   - BF: `jenkins.bingo-testing.elitescastle.com`, user=api
   - BV: `bingo2-jenkins.superbgame.net`, user=api
   
2. **GitHub PAT** - 同样从历史 session 找回
   - 用户: joycastle-jiuyi
   - Token: github_pat_11AWD6QCA0...（九毅的 fine-grained token）

3. **Vertex 凭证** - 本来就在 `~/.clawdbot/credentials/google-vertex-sa.json`，没丢

### 新规范

脚本/凭证不要散装放，统一塞飞书扩展：
- Jenkins 脚本: `~/.clawdbot/extensions/feishu/scripts/jenkins/`
- Git remote 已配好 token，可以直接 push

### 通知

已通知 BF/BV 研发大群：Jenkins 重启能力恢复。

## 定时重启指令

**问题：** 脚本丢了之后，群里说"30s后重启开发服"我就不认识了。

**正确处理：**
- "30s后重启开发服" → 设 cron job / sleep 后执行重启脚本
- 不是让我等 30s 再回复，是让我设定时任务

已更新 `memory/tools/jenkins.md` 添加指令识别规则。

## 技术备忘

Session 历史是个宝库，丢东西先去那儿翻。
