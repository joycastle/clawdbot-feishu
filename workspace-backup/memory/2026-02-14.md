# 2026-02-14 摘要
- Claude 429 限流事件：多个 session 上下文膨胀（贾诩cron 172k、群聊130k、宪伟120k）+ 重复cron任务导致 TPM 触发
- security-scan 和 system-monitor 两个 cron job 因 `gemini-3-flash-preview` model not allowed 连续报错（9次/7次）
- 贾诩推送任务有两个重复的（eaab6ea3 和 3911486f），需删一个
- 宝根要求排查 compaction 问题，确认是 session 没及时压缩导致单次请求过大
- 修复动作：删除重复贾诩cron(3911486f)、禁用security-scan和system-monitor（gemini模型不可用）
- compact宪伟session(120k)成功、群聊(130k)成功、贾诩cron(172k)触发连续429
- 确认：OpenClaw cron isolated session 不会自动 compact，是设计缺陷
- **已修复** gemini-3-flash-preview：添加到 `agents.defaults.model.fallbacks` 配置中，根因是 `buildAllowedModelSet()` 只扫 model.primary/fallbacks/models，不扫 imageModel
- security-scan 和 system-monitor cron 已重新启用
- OpenClaw auto-compaction 阈值：contextTokens(200k) - reserveTokensFloor(100k) = 约100k时触发
- 宝根要求测试 auto-compaction：故意填充上下文到阈值观察行为（进行中）
- 绿洲私聊：请求优化11种颜色的区分度+排序（让任意5色差异最大化），回复了一张图但我看不到，已请他重发
- **Gemini allowed-set 彻底修复**：清空 `agents.defaults.models` map 启用 allowAny 模式，子代理测试 gemini-3-flash-preview 成功（4秒完成）
- **QMD 现在能用了！** 之前 OOM kill 可能是内存被占满，当前 15GB RAM / 13GB 可用，查询正常返回。可考虑启用为默认 memory 搜索后端
