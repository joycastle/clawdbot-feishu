# 2026-02-26 宝根私聊

## Compaction 问题调查与修复

### 问题
- Session 上下文突然断裂，summary 显示 "Summary unavailable due to context limits"
- 历史丢失但没有真正的摘要

### 根因（与 GPT 对齐）
- `ctx.model === undefined` 导致直接走 fallback
- 不是模型调用失败，是根本没调用就返回了
- 证据：25ms 时间差（真调 Opus 要几秒）

### 修复
- 应用 GPT 补丁：runtime registry 注入 model
- `model = ctx.model ?? runtime?.model`
- 缺失时 `{ cancel: true }` 取消压缩，保留历史
- 加了调试日志，等下次自然触发观察

### 记忆层反馈
- 宝根指出我对记忆使用不够主动
- 2/12 后 14 天没写过 session 独立记忆
- 已更新 AGENTS.md 强化主动记忆规则

## 关键教训
- **主动记忆**：不等提醒，每次对话后自问是否需要记录
- **层级判断**：跨 session 通用的内容必须同步到 MEMORY.md
