# 2026-01-30 工作日志

## 何森 (ou_841797e6c6009a30dddb49dd7276bb6c) — 飞书插件开发

### 上下文隔离功能 (contextIsolation)
- 已实现并提交：`e013916 feat: context isolation per user, group, and topic`
- Session key 路由：DM per-user, group per-chatId, thread per-rootId
- 配置项：`channels.feishu.contextIsolation`（默认 true）
- 何森开始测试

### 模型路由功能 (modelRouter)
- **需求**：正常聊天用 Gemini，开发/技术类请求切换到 Claude Opus
- **已实现**：新建 `src/model-router.ts`（意图检测）+ `src/model-switch.ts`（确认卡片+session model override）
- 修改了 config-schema / types / bot / monitor / channel 共7个文件
- 已提交并推送到 `joycastle/clawdbot-feishu` 仓库 feature/joycastle 分支：`d3d8fe1`
- Git remote 用的是何森提供的 PAT token（joycastle-jiuyi 用户）

### Gemini/Vertex AI 配置（进行中）
- 何森想用 **Google Vertex AI + Service Account** 认证（不是 Google AI Studio API Key）
- SA JSON 来自 project `larkbot-485707`，client_email: `openclaw@larkbot-485707.iam.gserviceaccount.com`
- **已验证可用**：SA 认证拿到 token ✅，Vertex AI 调用 gemini-2.5-flash-lite 成功 ✅

#### clawdbot Vertex AI 认证机制（源码分析）
- Provider: `google-vertex`
- 认证检测需要三个条件同时满足：
  1. `GOOGLE_APPLICATION_CREDENTIALS` 环境变量指向 SA JSON 文件
  2. `GOOGLE_CLOUD_PROJECT` 环境变量
  3. `GOOGLE_CLOUD_LOCATION` 环境变量
- auth-profiles.json 里需要 `google-vertex:default` profile（type=api_key，只是占位）
- 实际认证走 ADC（Application Default Credentials）

#### 当前配置状态（未重启）
- systemd service: 3个环境变量已加
- SA JSON: `/home/ubuntu/.clawdbot/credentials/google-vertex-sa.json`（权限600）
- auth-profiles.json: `google-vertex:default` ✅
- clawdbot.json: provider=google-vertex, model=google-vertex/gemini-2.5-flash-lite ✅
- clawdbot.json 备份: `.bak.20260130_140039`
- **等待何森确认后重启**

### modelRouter 配置
```json
"modelRouter": {
  "enabled": true,
  "defaultModel": "google-vertex/gemini-2.5-flash-lite",
  "devModel": "anthropic/claude-opus-4",
  "autoConfirm": false
}
```

### auth-profiles type 改为 oauth
- 何森选择方案2：保留 profile，type/mode 改成 `oauth`（语义更准确）
- auth-profiles.json: `google-vertex:default.type` → `oauth`
- clawdbot.json: `google-vertex:default.mode` → `oauth`
- 实际认证仍走 ADC（环境变量），profile 解析会尝试 OAuth token refresh 失败后 fallback

### GOOGLE_CLOUD_LOCATION 环境变量问题
- systemd service 文件有3个 GOOGLE_* 环境变量
- 但 SIGUSR1 重启（进程内重启）继承旧 env，新加的 `GOOGLE_CLOUD_LOCATION` 没被加载
- **修复**：`systemctl --user restart clawdbot-gateway` 完整重启后三个变量都齐了
- **教训**：改了 systemd service 的 Environment 后必须做完整 systemd 重启，SIGUSR1 不够

### modelRouter 改为显式切换（去掉确认卡片）
- 何森要求：去掉确认卡片，默认 Gemini，用户明确说"开发"/"切换claude"时才切换
- `model-router.ts`：去掉大量 dev 关键词检测，改为只匹配明确切换请求
  - 切到 Claude："开发"、"切换到claude"、"用opus"等
  - 切回 Gemini："聊天"、"切换到gemini"、"退出开发"等
  - 新增 `shouldSwitchToDefault` 字段支持切回默认模型
- `bot.ts`：去掉确认卡片流程，直接切换
- config: `autoConfirm: true`, `defaultModel: google-vertex/gemini-2.5-flash-lite`, `devModel: anthropic/claude-opus-4-5`

### 注意事项
- 之前用错了 Google AI Studio API key 格式（AQ.Ab8RN... 不是 AIza... 格式），导致 401 错误
- 何森问过能否用 service account → 可以，通过 Vertex AI provider
- clawdbot 自更新可能覆盖 systemd service 文件，环境变量可能丢失（何森说直接改 service 文件就行）
- 飞书插件是 TS 源码，clawdbot 加载器内置 tsx 支持，改代码后直接重启即可，不需要编译
