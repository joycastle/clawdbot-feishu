# 2026-02-04

## 新用户 ou_2f460687acb188f14f3c8d6af3fd30e7
- 未在 USER.md 已知用户列表中，不知道名字
- 技术背景很好，了解前后端、CI/CD、流式传输、Node.js 异步模型、背压等概念
- 今天做了大量飞书插件架构和视频分析方案的深度技术讨论
- 提到有 mentor，mentor 就是何森（负责配置 Vertex AI SA 等）

## 大视频分析方案（已完成编码，待测试）

### 最终方案（经多轮讨论，方案从 Files API → GCS 中转方向演进）
- 初始讨论想用 Gemini Files API（Google AI Studio），后来确认走 GCS 中转 + Vertex AI
- 原因：已有 Vertex AI SA 凭证，GCS 直接复用，不需新增 API Key

### 核心配置
- 多维表格 app_token: OW7lbIpSlaf4nEsiDKLcqiYGn7c
- 多维表格 table_token: tblPFJHzLTyXMGcJ
- 表格字段：自动编号 + 附件
- GCS bucket: larkbot-storage（已验证读写删除 OK）
- 认证：复用 Vertex AI SA（openclaw@larkbot-485707.iam.gserviceaccount.com）
- 需要飞书权限：bitable:app:readonly + drive:media:readonly

### 用户交互
- "视频：最新，请xxx" 或 "视频：编号为X的，请xxx"
- 小于 20MB 视频直接通过飞书消息处理
- 大于 20MB 视频提示上传到多维表格

### 技术架构
- 双层缓存：filetoken→gcsUri + md5→gcsUri（7天 TTL）
- 流式处理：飞书下载→/tmp 临时文件（同时算 MD5）→GCS 分块上传（8MB/chunk）→Vertex AI
- /tmp 临时文件用完即删，不长存
- 每日午夜自动清空多维表格记录
- 每 7 天清空缓存映射和 GCS 桶

### 代码变更（分支 feature/joycastle-video）
- 新建 4 文件：bitable-video.ts, gcs-upload.ts, video-cache.ts, bitable-video-handler.ts
- 修改 3 文件：video-analyze.ts（加 analyzeVideoFromGcs）, bot.ts（拦截"视频："指令）, index.ts
- 用户额外提交改进：
  - gcs-upload.ts 分块上传优化（8MB/chunk + 重试），避免大文件内存爆炸
  - bitable-video.ts 新增 clearAllBitableRecords（分页删除）
  - channel.ts 注册每日定时清理任务
- 最新 commit: a02810b
- 状态：代码已 pull 到服务器，待重启测试

### 讨论中的关键技术决策记录
1. Gemini Files API vs GCS 中转 → 选 GCS（复用现有 SA，不需新 key）
2. 纯流式管道 vs /tmp 落盘 → 选 /tmp（可靠性好，两端传输独立，失败可重试）
3. MD5 去重 → 流式计算不炸内存，fileToken+MD5 双层缓存
4. 飞书消息 API ~20MB 限制 vs Drive API → Drive API 限制更宽松
5. Vertex AI inline 20MB 限制 → GCS URI 模式支持到 2GB

## 其他今日互动
- 帮用户搜了今天的科技新闻（SpaceX 收购 xAI、OpenAI Codex 桌面应用、ChatGPT 9.3 亿月活等）
- 总结了飞书开放平台三方审批教程页面
- 给用户解释了 Clawdbot + 飞书 + Gemini/Claude 的协作架构
- 确认了 GitHub 仓库 joycastle/clawdbot-feishu 的访问权限（PAT 只能看 public repos）
- 讨论了 CI/CD 流程：目前是手动 "告诉王总更新" 模式，可以加 webhook 自动化
